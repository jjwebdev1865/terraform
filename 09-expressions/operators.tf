locals {
  math       = 2 * 2          # math operators: *, /, +, -
  equality   = 2 == 2         # equality operators: ==, !=     #returns boolean for checks
  comparison = 2 > 1          # Comparison operators: <, <=
  logicatl   = !true || false # logical Operators: true, !true, false, &&, ||
}